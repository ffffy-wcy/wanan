# Checklist

- [x] Moment 模型已添加到 `prisma/schema.prisma` 且 `db push` 成功
- [x] Multer 已安装，图片上传 API 可正常接收文件并保存到 `uploads/` 目录
- [x] `GET /api/room/:roomId/moments` 返回按时间倒序的瞬间列表
- [x] `POST /api/room/:roomId/moments` 接受图片+文字+位置，创建记录并广播 WebSocket 事件
- [x] `DELETE /api/room/:roomId/moments/:id` 仅允许创建者删除，删除后广播事件
- [x] Leaflet 地图在"之间"页面正确渲染，支持拖动和缩放
- [x] 双方位置标记显示为圆形头像标记（昵称首字），中间有虚线连接
- [x] 距离标签实时显示在地图上方
- [x] WebSocket `location:update` 事件触发地图标记和距离的实时更新
- [x] 只有一方有位置时，地图仅显示该方标记，无连线
- [x] 双方都无位置时，地图显示默认视图和提示文字
- [x] 底部导航新增"瞬" tab，点击可切换
- [x] "瞬"页面时间线按时间倒序展示双方瞬间
- [x] 创建瞬间：选择照片 → 填写文字 → 自动获取位置 → 提交成功
- [x] 点击瞬间可查看大图和完整信息
- [x] 删除瞬间需确认，仅发布者可删除
- [x] WebSocket 实时同步：对方发布/删除瞬间后本地自动更新
- [x] 空状态引导文案正确显示
- [x] 加载态、错误态、重试机制覆盖所有新增数据请求
- [x] 暗色模式下地图和"瞬"页面样式正常
- [x] 移动端 320px-428px 宽度下 UI 布局正常
- [x] 服务器启动正常，页面加载完整